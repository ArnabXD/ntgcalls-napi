[package]
name = "ntgcalls"
version = "0.3.0-beta-04"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "3", features = ["async", "tokio_rt", "napi6"] }
napi-derive = "3"
tokio = { version = "1", features = ["sync"] }

[build-dependencies]
napi-build = "2"
